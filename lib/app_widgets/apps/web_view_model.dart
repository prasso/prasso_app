class WebViewModel {
  WebViewModel(String pageTitle, String pageUrl, String headerInfo) {
    pageUrl = pageUrl;
    pageTitle = pageTitle;
    headerInfo = headerInfo;
  }

  String pageUrl = '';
  String pageTitle = '';
  String headerInfo = '';
}
